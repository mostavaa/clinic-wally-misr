@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <div class="row">
        <div class="col-md-2">@Html.ActionLink("Employees", "Index", "SystemPersons")</div>
        <div class="col-md-2">@Html.ActionLink("Patients", "Index", "patients")</div>
        <div class="col-md-2">@Html.ActionLink("Medicines", "Index", "medicines")</div>
        <div class="col-md-2">@Html.ActionLink("Constants", "constants")</div>
        <div class="col-md-2"></div>
        <div class="col-md-2"></div>
    </div>
</div>

